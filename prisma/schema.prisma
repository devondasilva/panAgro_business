datasource db {
  provider = "postgresql"
}

generator client {
  provider = "prisma-client-js"
}

// Tes modèles restent inchangés ici
model User {
  id    Int    @id @default(autoincrement())
  email String @unique
}